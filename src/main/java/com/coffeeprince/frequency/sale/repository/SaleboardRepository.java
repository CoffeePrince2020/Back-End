package com.coffeeprince.frequency.sale.repository;

import com.coffeeprince.frequency.sale.domain.SaleBoard;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SaleboardRepository extends JpaRepository<SaleBoard, Long> {
}
